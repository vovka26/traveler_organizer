<h1> <%= @activity.name %></h1>

<div>
  <%= image_tag(@activity.img_url) %>
</div>
<div>
  <%= form_tag @activity, method: "DELETE", data: {confirm: "Delete this activity?"} do %>
      <%= submit_tag "Delete this activity" %>
  <% end %>
</div>
<br><br>
<div>
  <%= link_to "All activities", activities_path %>
</div>
